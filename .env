# Copy this file to .env and adjust values before running docker-compose.
# .env is gitignored — never commit real credentials.

SA_PASSWORD=StrongPass123

# Directory containing the CSV file (defaults to project root)
CSV_DIR=.

# CSV filename inside CSV_DIR (defaults to sample-cab-data.csv)
CSV_FILE=sample-cab-data.csv
